import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginLeft: 8,
    backgroundColor: 'grey',
    paddingHorizontal: 8,
    paddingVertical: 2,
    borderRadius: 99,
  }
});